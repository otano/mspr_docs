# Guide rapide

Ce guide explique comment éditer la documentation et la déployer.

## Éditer

- Modifier les fichiers dans `docs/`.
- Mettre à jour `mkdocs.yml` si nécessaire.

## Construire localement

```bash
pip install -r requirements.txt
mkdocs build
```
